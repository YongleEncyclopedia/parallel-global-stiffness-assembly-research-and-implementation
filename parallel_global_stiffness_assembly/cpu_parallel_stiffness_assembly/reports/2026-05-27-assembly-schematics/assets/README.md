# Assets

本目录保存 `scripts/plot_assembly_schematics.py` 生成的组装路径示意图，以及对应的 `source_manifest.json`。

工作树保留三种格式：

- `.svg`：可编辑文本的矢量版本。
- `.pdf`：矢量 PDF。
- `.png`：预览图。

脚本仍可按需生成 `.tiff` 归档/投稿候选位图，但该可重建格式不纳入 Git 工作树。
